import React from 'react';
import './MenuBar.css';

const MenuBar = () => {
    return (
        <div className="menu-bar">
            {/* Add your menu items here */}
        </div>
    );
};

export default MenuBar;